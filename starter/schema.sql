-- Your database schema for your chosen domain.
-- Design this AFTER filling in answers.md's schema-sketch section, not before.
--
-- Requirements (see MVP.md on the project-scope branch for the full bar):
--   - 3 tables minimum, matching the sketch you already wrote
--   - normalized to AT LEAST 3NF -- see answers.md's schema-sketch section
--     for what that actually means per table
--   - a real PRIMARY KEY on every table
--   - real FOREIGN KEY constraints expressing the actual relationships in
--     your data (see data/SOURCE.md for which column really links to which)
--   - at least one NOT NULL or CHECK constraint per table, chosen on
--     purpose, not just copied onto every column
--
-- Run this whole file via Python (see GETTING_STARTED.md and starter/db.py)
-- -- not `psql`:
--   python3 starter/db.py starter/schema.sql

-- TODO: CREATE TABLE statement for your first (parent/lookup) table

-- TODO: CREATE TABLE statement for your second table

-- TODO: CREATE TABLE statement for your third table (add a 4th/5th here
-- too, if you're extending this for Above & Beyond)
